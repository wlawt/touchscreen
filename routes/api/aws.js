const express = require("express");
const router = express.Router();

/*  @route      POST api/aws
    @desc       Send req to AWS server
    @access     Public
*/
router.post("/", (req, res) => {
  // Do stuff here.
});

module.exports = router;
